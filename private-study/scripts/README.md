# Scripts

This folder is reserved for export and admin utilities.

Planned scripts:

- Export `task_session_completions` as CSV or JSON for analysis.
- Apply admin/developer overrides to `participant_study_settings.drug_day`.
- Backfill `participant_randomizations` for imported participants.
